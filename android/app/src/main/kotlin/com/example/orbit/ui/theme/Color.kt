package com.example.orbit.ui.theme

import androidx.compose.ui.graphics.Color

// Mockup Redesign Palette
val OrbitBackgroundDark = Color(0xFF000000) // Pitch Black
val OrbitBackgroundLight = Color(0xFF000000) // Pitch Black
val OrbitSurface = Color(0xFF161B22) // Base Slate for cards, though GlassmorphicCard will apply alpha
val OrbitSurfaceVariant = Color(0xFF1E293B) // Slightly lighter slate
val OrbitAccent = Color(0xFF38BDF8) // Cyan (Primary branding, FABs, text)
val OrbitAccentSecondary = Color(0xFF10B981) // Emerald Green (Toggles, Dismiss, Active Nav)
val OrbitWarning = Color(0xFFF59E0B) // Orange (Snooze, Tags)
val OrbitError = Color(0xFFF43F5E) // Red (High tags)
val OrbitTeal = Color(0xFF00BFA5) // Kept for generic teal needs if any
val OrbitTextPrimary = Color(0xFFFFFFFF) // Pure White
val OrbitTextSecondary = Color(0xFF94A3B8) // Slate Gray text
val OrbitGlassBorder = Color.Transparent
